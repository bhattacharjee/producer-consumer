1. Run the binary
        erl

2. In the erlang prompt, compile. (Don't forget the full-stop).
        compile:file(prodcon).

3. Run the program
        prodcon:start().

Note: This can be converted to an escript that doesn't require
interactive activity with the erlang shell.
